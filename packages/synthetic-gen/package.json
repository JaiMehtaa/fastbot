{
  "name": "@whatsapp-bot-platform/synthetic-gen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "tsc -b && node --test dist/*.test.js"
  },
  "dependencies": {
    "@whatsapp-bot-platform/shared-types": "workspace:*",
    "@whatsapp-bot-platform/schema": "workspace:*",
    "@whatsapp-bot-platform/compiler": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "@types/node": "^26.1.1"
  }
}
