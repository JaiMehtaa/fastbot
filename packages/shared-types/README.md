# packages/shared-types — Cross-App TypeScript Types

Types shared across `apps/*` and `packages/*` so nothing drifts between the interview API, compiler, and runtime — e.g. `DraftConfig`, `CompiledConfig`, `PrimitiveSchema`, `InboundMessage`, `ConversationState`, `DashboardNotification`.
