# All optional — only needed if you're not running the other services on
# their default local ports (interview-api: 3001, runtime: 3002, dashboard: 3003).

INTERVIEW_API_URL=http://localhost:3001
RUNTIME_API_URL=http://localhost:3002
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3003
